import 'package:flutter/material.dart';

class AISearchScreen extends StatelessWidget {

 static const String routeName = "/aiSearch";

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}